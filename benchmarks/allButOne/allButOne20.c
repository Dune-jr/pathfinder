/* Program with 1 (expensive?) infeasible path.
   Contains 2^20 syntactic paths. */
int main(int argc, char *argv[]) {
  int x = 0;
  int c1, c2, c3, c4, c5, c6, c7, c8, c9, c10, c11, c12, c13, c14, c15, c16, c17, c18, c19, c20;
  if (c1) { x++; }
  if (c2) { x++; }
  if (c3) { x++; }
  if (c4) { x++; }
  if (c5) { x++; }
  if (c6) { x++; }
  if (c7) { x++; }
  if (c8) { x++; }
  if (c9) { x++; }
  if (c10) { x++; }
  if (c11) { x++; }
  if (c12) { x++; }
  if (c13) { x++; }
  if (c14) { x++; }
  if (c15) { x++; }
  if (c16) { x++; }
  if (c17) { x++; }
  if (c18) { x++; }
  if (c19) { x++; }
  if (!(c1 && c2 && c3 && c4 && c5 && c6 && c7 && c8 && c9 && c10 && c11 && c12 && c13 && c14 && c15 && c16 && c17 && c18 && c19 && c20)) { x++; }
  return x;
}
